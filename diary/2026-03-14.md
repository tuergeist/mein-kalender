# 2026-03-14

- Fixed nginx reverse proxy routing: `/api/auth/*` goes to NextAuth (Next.js), `/api/auth/register` and `/api/auth/login` go to Fastify API
- Fixed ESLint config: migrated from FlatCompat to native typescript-eslint for TS file parsing
- Fixed OAuth redirect URLs to use `window.location.origin` so ngrok URLs work correctly
- CI/CD still failing: build errors in API package — missing `getConsentUrl` on CalendarProviderInterface, implicit `any` types in oauth.ts and providers
- Working on fixing TypeScript compilation errors to get CI green
