# 2026-04-04

## Calendar view cleanup
- Removed "[Sync] ausblenden" toggle — always hide [Sync] events (they don't reliably appear in our DB due to Google delta API not returning self-created events)
- Removed Sync button from sidebar, replaced with "Alle anzeigen" / "Nur aktive" toggle for disabled calendars
- Fixed sidebar toggle labels to be action-oriented

## Event detail modal redesign
- LocationMap: detect URLs AND text like "Microsoft Teams Meeting" — no map for online meetings
- Date format shortened: "9.4.2026 16:00 – 17:00" instead of full locale with seconds
- New DayTimeline component: horizontal 7-21h bar with colored event blocks, highlighted current event, now indicator

## Sync UI improvements
- Removed misleading "Sync-Ziel" badge from source calendar list
- Simplified target card labels
- Redesigned sync target cards: 2-column "Quellen → Zielkalender" layout, click-to-edit, inline Cancel/Save/Delete buttons
- userColor now used in sync page calendar lists (matching sidebar colors)
- Disabled calendars shown at 40% opacity but remain selectable

## Conflict deep-linking
- Clicking a conflict now opens day view (not week) at the right time
- Both conflicting events get a red 2px outline for instant identification
- Ignoring an event immediately recomputes conflicts (was waiting for next sync)

## Pricing update
- Landing page: show both 19 EUR/month and 190 EUR/year (save 17%)

## Clone stats on sync-status page
- New API endpoint GET /api/dashboard/clone-stats
- Shows per-target: total mappings, last sync time, sync mode

## Redis orphan cleanup
- 1603 orphaned Redis keys from deleted calendar sources cleaned up
- Worker now auto-removes orphaned BullMQ schedulers on startup by comparing against DB sources
- This eliminated persistent "Source not found, skipping" log noise

## Admin setup
- Promoted mail@ch-becker.de to admin role, disabled admin@admin.local
- Admin panel exists at /admin with user management, sources overview, sync jobs

## Payment integration research
- Starting Mollie integration for monthly (19 EUR) and yearly (190 EUR) subscriptions
