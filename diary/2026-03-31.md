# 2026-03-31

## Conflict system overhaul

The dashboard conflict display had a fundamental design mismatch: the "Überschneidungen" card showed `overlapsDetected` (weekly count, includes resolved), but the banner showed unresolved conflicts capped at 3. User saw 5 vs 3 — confusing.

**Decision:** Moved conflict details to a dedicated `/conflicts` page instead of cluttering the dashboard. Dashboard now only shows the count (from briefing endpoint), linking to the detail page. This also removed one API call from the dashboard load.

**New capability:** `/calendar?date=YYYY-MM-DD` — CalendarView now accepts `initialDate` prop so conflict links can deep-link to the right day.

**Manual resolve:** Added `POST /api/dashboard/conflicts/:id/resolve` because auto-resolution only runs during sync. Users can now dismiss conflicts immediately via an "Erledigt" button.

## Booking invite bug

Discovered that booking events were created without attendees — guest email was only in the description text. The attendee infrastructure existed in both Google and Outlook providers (`toGraphEvent`/`toGoogleEvent` both handle attendees, Google even has `sendUpdates=all`), but `public-booking.ts` never passed the `attendees` array. One-line fix: added `attendees: [{ email: guestEmail, name: guestName }]` to the createEvent call.

## Feature batch (4 parallel sub-agents)

Ran 4 features in parallel worktrees. Two agents couldn't create new files (Write permission denied in worktree), so I merged their backend changes and built the frontend manually.

**Guest self-service management** (p2): Biggest feature. Token-based booking management — guests get a `/book/manage/:token` link in the calendar event. Can cancel (deletes provider event) or reschedule (date picker → slots → recreates event). Used `crypto.randomUUID()` for tokens, reused existing `computeSlots` for availability. `findBookingCalendarEntry` helper extracted for reuse across cancel/reschedule.

**User calendar colors**: Added `userColor` to CalendarEntry. Sidebar color dot is now clickable → opens 11-color popover palette. Optimistic UI, persists via `PATCH /api/calendar-entries/:id/color`. CalendarView uses `userColor ?? provider color`. Custom event `calendar-color-change` triggers re-fetch.

**Proton ICS help**: Updated Proton settings instructions to match current Proton UI (account.proton.me → Kalender-Einstellungen → Aktionen). Added collapsible help in ICS URL input.

**Sync status page**: New `/sync-status` page with per-source status and weekly metrics. Dashboard sync card now links there; removed collapsible inline details.

**CI fix**: `useSearchParams()` on calendar page needed `<Suspense>` boundary for Next.js static generation.
