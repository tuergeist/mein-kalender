# 2026-03-21

## Session: Convert heavy modals to dedicated pages

### Key decisions and discoveries

**Modal → page threshold:** Event type form (7 sections, ~15 fields) was too large for a modal — converted to dedicated pages at `/settings/booking/new` and `/settings/booking/[id]/edit`. Each section is its own Card. ICS feed form (4 fields) was borderline — went with inline expandable form inside the existing card rather than a full page, since it's small enough and avoids a context switch from the sync page.

**Kept modals for:** Unset target calendar confirmation, event detail view on calendar (quick peek pattern needs to stay modal). Small confirmations are fine as modals.

**Build was already broken:** `FAST_JWT_INVALID_OPTION` error during `next build` — missing JWT key env var. Pre-existing, not related to today's changes. `tsc --noEmit` passes clean.

### Changes made
- New page: `/settings/booking/new` — event type creation with 6 card sections
- New page: `/settings/booking/[id]/edit` — event type editing, loads existing data via API
- Booking settings page: removed modal and all form state, buttons now `<Link>` navigations
- Sync page: ICS feed modal replaced with inline expandable form (highlighted border, stays in context)
