# 2026-04-09

## Color picker overflow fix
Sidebar color picker for the last calendar entry gets clipped by the sidebar overflow. Fixed: open upward (bottom-full) instead of downward.

## Booking fixes
- Guest email validation added (frontend + backend) — was accepting "eqwe" as email
- Manage URL corrected from `meinkalender.link` to `app.mein-kalender.link`
- Cancelled booking page: removed redundant "Diese Buchung wurde storniert", added "Neuen Termin suchen" link

## Mollie deployment crash
API pods crashed on deploy because `@mollie/api-client` throws at import time if `MOLLIE_API_KEY` is not set. Fixed by lazy-initializing the client (function call instead of top-level const). Old crashed pods were stuck in CrashLoopBackOff blocking new rollouts — had to manually rollback + set image.

## Mollie integration — PICKUP POINT

Code is fully implemented and deployed (commit 502a925 + 5ef8141). What remains:

1. **Create Mollie account** at mollie.com
2. **Get test API key** from Mollie Dashboard
3. **Set env variable**: `kubectl set env deployment/api -n mein-kalender MOLLIE_API_KEY=test_xxxxx`
4. **Test the flow**: /settings/billing → checkout → Mollie test checkout → webhook → subscription active
5. **Before go-live**: replace test key with live key, complete Mollie account verification (website, bank, ID — up to 10 business days)

Endpoints ready:
- `POST /api/checkout` — starts first payment, returns checkoutUrl
- `POST /api/webhooks/mollie` — handles payment status, creates subscription
- `GET /api/billing` — returns plan, status, trialEndsAt, isActive
- `POST /api/billing/cancel` — cancels subscription

Frontend ready:
- `/settings/billing` — pricing cards, subscription status, cancel flow
- Dashboard trial banner — "Testphase: noch X Tage"
- "Abrechnung" in settings sub-nav
