# 2026-08-31

## Release des Sync-Status

Die Änderungen vom Vortag ausgerollt, CI und k8s-Deployment grün, ohne Fehlerrunde. Mitgenommen: der `TODOS.md`-Abschnitt zu Billing, der von Stripe auf den Mollie-Stand umgeschrieben war und uncommittet herumlag. Nicht mitgenommen: `package-lock.json` — die Änderung setzt ausschließlich `peer: true`-Markierungen um, kein Paket und keine Version ändert sich. Das ist Rauschen aus einem `npm install` mit anderer npm-Version.

## Reclaim.ai-Termine auf Buchungsseiten

Im Produktionskalender liegen Termine, die Reclaim einträgt und die Buchungszeiten sperren. Vor dem Bauen in der Produktionsdatenbank nachgesehen, lesend:

| Art | Google-Status | Anzahl | zukünftig |
|---|---|---|---|
| Aufgabe über Reclaim (aus Todoist) | frei | 34 | 34 |
| Aufgabe über Reclaim | belegt | 11 | 9 |
| Reclaim-Eigenblöcke (AI-powered) | belegt | 11 | 11 |

Die 34 als frei markierten blockieren schon vorher nichts — der Slot-Rechner wirft `transparency: transparent` seit jeher raus. Es ging also um 20 zukünftige Termine, nicht um 56.

**Die Erkennung hängt am Link, nicht am Text.** Der Nutzer nannte „This event was created by Reclaim" als Marker; in der Datenbank steht dieser Wortlaut aber in keinem einzigen Datensatz, weil Reclaim bei den Eigenblöcken „This **AI-powered** event was created by…" schreibt. Gemeinsam ist beiden Formen nur der `reclaim.ai`-Link in der Beschreibung. Der trifft alle 56 und nichts sonst. Genau an „AI-powered" hängt auch die Unterscheidung der beiden Arten — geprüft an diesen 56 Terminen, dokumentiert ist Reclaims Formulierung nicht.

**Zwei Schalter statt einem**, so entschieden vom Nutzer: Aufgabenblöcke und Eigenblöcke getrennt abschaltbar, weil Fokuszeit und Sport weiter schützen sollen können, während Aufgabenblöcke buchbar bleiben.

**Gefiltert wird beim Rechnen, nicht in der Datenbank.** Der naheliegende Weg wäre gewesen, beim Sync `ignored` zu setzen — das Feld gibt es, und die Slot-Berechnung wertet es aus. Dagegen sprach: Ein Ausschalten hätte einen Re-Sync gebraucht, und `ignored` wirkt auch auf Konflikterkennung und Kalender-Clone, die hier nicht gemeint sind.

Eine Prisma-Variante, die verlockend aussah und falsch gewesen wäre: `description: { not: { contains: "reclaim.ai" } }` in der Abfrage. `NOT (description ILIKE …)` ist für `NULL` nicht wahr, also wären alle Termine ohne Beschreibung mit herausgefallen. Deshalb Beschreibung mitladen und in JavaScript filtern.

**Nebenbefund:** Die Verfügbarkeitsvorschau der Terminart lädt ihre Belegt-Balken mit einer eigenen Abfrage, die `ignored` nicht auswertet — von Hand ignorierte Termine erscheinen dort weiterhin als Balken über einer buchbaren Zeit. Für Reclaim habe ich den Filter in beide Pfade gelegt; die `ignored`-Abweichung ist älter und steht noch offen.

## Warum die Buchungsseite erst ab 13:30 anbot

Der Nutzer meldete, dass `/book/cbx/30-minuten-kennenlernen` am 1. und 2.9. erst ab 13 Uhr Zeiten zeigt. Die Live-Antwort der API bestätigte es: erster Slot 11:30 UTC an beiden Tagen. Drei Ursachen, die sich addieren, und die größte hat mit Reclaim nichts zu tun:

**Arbeitszeiten wurden als UTC gerechnet.** `computeSlots` setzte die Regelzeiten mit `setUTCHours` an und las `User.timezone` nie — obwohl das Feld existiert, auf `Europe/Berlin` steht und die Buchungsseite dem Besucher genau diese Zone anzeigt. Aus 09:00–17:00 wurden faktisch 11:00–19:00 Berlin. Vor 11:00 konnte nie etwas erscheinen.

Dazu: Der 15-Minuten-Vorlauf muss in die Arbeitszeit passen, das kostet den 11:00-Slot. Und „🍱 Lunch" (12:00–13:00 Berlin, belegt) sperrt mit Puffern 11:45–13:15. Bleibt 13:30 — genau die Ausgabe.

Umgerechnet wird jetzt über `Intl.DateTimeFormat` in `lib/timezone.ts`, in zwei Durchgängen: Der Versatz hängt vom Zeitpunkt ab, den man erst kennt, wenn man umgerechnet hat. Ein Durchgang wäre an den zwei Umstellungstagen im Jahr falsch. Keine Bibliothek, Node bringt die Zonendaten mit.

**Zweites Loch: Gewohnheiten ohne Beschreibung.** Genau der Lunch-Block ist ein Reclaim-Termin, hat aber gar keine Beschreibung — der Marker griff nur im Beschreibungstext. 16 solche Termine im Konto, alle „🍱 Lunch". Reclaim stellt bei ihnen 🛡 (U+1F6E1) oder 🆓 (U+1F193) an den Titelanfang. Umgekehrt tragen 15 andere Reclaim-Termine kein Emoji, aber den Link. Es braucht also beide Merkmale; die Erkennung prüft jetzt Beschreibung ODER Titelanfang. Fehlt die Beschreibung, gilt der Termin als Eigenblock, weil eingeplante Aufgaben in den geprüften Daten immer eine haben.

**Darstellung:** Übergangene Termine bleiben im Kalender sichtbar, aber ohne Füllung und mit rot gestricheltem Rahmen. Dafür rechnet `/api/events` das Kennzeichen `bookingIgnored` mit, statt die Regel im Frontend zu wiederholen.
